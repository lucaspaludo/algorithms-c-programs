#include <stdio.h>

int main(void) {
  
  int num, i;
  int fact=1;
  i=1;
  printf("Digite um Número: ");
  scanf("%d", &num);

  while(i<=num)
  {
    fact = fact*i;
    i++;
  }
  
  printf("\n%d", fact);
  return 0;
}
