#include <stdio.h>

void fibonacci(int f_1, int f_2, int n){
if(n>0){
  int f = f_1 + f_2;
  printf("%d\n", f);
  fibonacci(f,f_1,n-1);
 }
}

int main(void){

printf("0");
printf("\n1\n");
fibonacci(1,0,18);
return 0;
}
